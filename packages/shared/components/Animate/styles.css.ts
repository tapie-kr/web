import { style } from '@vanilla-extract/css'

export const animate = style({
  display: 'block',
  width: 'fit-content',
  height: 'fit-content',
})

export const fullWidth = style({
  width: '100%',
})
